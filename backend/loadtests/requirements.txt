# Зависимости только нагрузочных тестов. Сознательно отдельным файлом, а не в
# backend/requirements.txt: locust на сервере не нужен, а requirements.txt в CI
# проверяется pip-audit — тащить туда транзитивные зависимости инструмента,
# который никогда не поедет в прод, значит расширять поверхность CVE впустую.
# (Плюс requirements.txt лежит в UTF-16 LE с BOM — этот файл в обычном UTF-8.)
#
# Версия не приколочена намертво: файл не участвует в аудите и ставится вручную.
locust>=2.32,<3
